# Network Configuration
NETWORK="testnet" # "testnet" or "mainnet"

# Injective RPC Endpoints (public testnet endpoints)
INJECTIVE_RPC_ENDPOINT="https://testnet.tm.injective.network"
INJECTIVE_LCD_ENDPOINT="https://testnet.lcd.injective.network"
INJECTIVE_INDEXER_ENDPOINT="https://testnet.api.injective.network"
INJECTIVE_EXPLORER_ENDPOINT="https://testnet.explorer.injective.network"

# Wallet Configuration
# WARNING: Never commit your actual private key to version control
# This is a placeholder for educational purposes only
INJECTIVE_PRIVATE_KEY="your_private_key_here"

# API Configuration
PORT=3001
API_BASE_PATH="/api/v1"
CORS_ORIGIN="*"  # For production, set to your frontend domain

# Authentication (if required)
JWT_SECRET="your_jwt_secret_here"
JWT_EXPIRES_IN="1d"

# Database Configuration (if using a database)
# DB_URI="mongodb://localhost:27017/darwinfi"

# Logging Configuration
LOG_LEVEL="info"  # debug, info, warn, error

# Service Timeout Configuration (in milliseconds)
REQUEST_TIMEOUT=30000

# Rate Limiting
RATE_LIMIT_WINDOW=15 * 60 * 1000  # 15 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS=100  # Maximum requests per window

